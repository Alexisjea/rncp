{% extends 'admin.html.twig' %}

{% block title %}Reinitisaliser votre mot de passe{% endblock %}

{% block body %}
    <h1>Reinitisaliser votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Reset mot de passe</button>
    {{ form_end(resetForm) }}
{% endblock %}
